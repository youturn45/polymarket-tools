# Security Notes

- Never log or print full private keys.
- Never commit `.env` files or decrypted secrets.
- Validate all user inputs before placing trades.
- Use test accounts/wallets for development.
